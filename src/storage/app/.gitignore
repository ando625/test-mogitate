# ======================================
# 環境ファイル・機密情報
# ======================================
.env                     # DBやAPIキーなどの環境情報
.env.*.backup            # 予備の.envファイルも無視

# ======================================
# データベース
# ======================================
docker/mysql/data/       # Docker MySQL の実データ
database/*.sqlite        # SQLiteファイル
database/*.db            # SQLite/DB ファイル（念のため）

# ======================================
# ビルド・依存関係
# ======================================
/vendor                  # Composerの依存ライブラリ
/node_modules            # npm/yarnの依存ライブラリ
/public/hot              # Laravel Mix hot reload用
/public/storage          # ストレージリンクの実体（画像は個別管理）
/storage                 # キャッシュ、ログ、セッション、アップロードなど

# ======================================
# 一時・ログ・キャッシュ
# ======================================
*.log
*.cache
*.tmp

# ======================================
# 上げたいもの（!で除外して追跡）
# ======================================
!.gitignore              # .gitignore 自体は追跡
!/database/migrations/   # マイグレーションは追跡
!/database/seeders/      # Seeder は追跡
!/app/Models/            # モデルは追跡
!/resources/views/       # Bladeファイルは追跡
!/public/css/            # CSS は追跡
!/public/js/             # JS は追跡
!/public/products/       # 画像（必要なものだけ）を追跡
!/public/images/

# 公開ディレクトリと .gitignore は無視しない
!public/
!.gitignore